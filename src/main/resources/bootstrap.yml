spring:
  application:
    name: customer-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: native
server:
  port: 8086   
    
eureka:
  client:
    service:
      url:
        default-zone: http://localhost:8761